{
  "name": "yuniruyuni.net",
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "npm-run-all2": "^6.2.2",
    "tailwindcss": "^3.4.6"
  },
  "scripts": {
    "build": "run-p 'build:*'",
    "build:html": "mkdir -p dist && cp index.html dist/index.html",
    "build:css": "NODE_ENV=production tailwindcss build -m index.css -o dist/index.css",
    "build:images": "mkdir -p dist && cp *.webp dist/"
  }
}